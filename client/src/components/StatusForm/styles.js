const createModalStyle = {
    position: 'absolute',
    top: '50%',
    left: '50%',
    transform: 'translate(-50%, -50%)',
    width: '33%',
    bgcolor: 'background.paper',
    border: '2px solid #000',
    boxShadow: 24,
    p: 4,
    height: '40%',
    overflowY: 'scroll',
    textAlign: 'center',
};

const inlineText = {
    display: 'inline',
}

const cellCard = {
    position: 'relative',
    height: '10%',
}

export { createModalStyle, inlineText, cellCard };